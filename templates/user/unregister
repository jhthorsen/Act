[% WRAPPER ui  title = loc('Un-Registration') %]

<form method="POST" action="[% global.request.r.uri %]">
  <p>
    [% loc('To unregister from <conference>,', global.conference.name) %]
  </p>
  <p>
    <button class="btn btn-danger" name="leave" value="[% loc('Unregister from <conference>', global.conference.name) %]">[% loc('Unregister from <conference>', global.conference.name) %]</button>
  </p>
</form>

[% END %]
